bool isPalindrome(int x) {
    if(x < 0)
        return false;

    int newx = x;
    long long ans = 0;

    while(x > 0)
    {
        int temp = x % 10;
        ans = (ans * 10) + temp;
        x = x / 10;
    }

    return newx == ans;
}
